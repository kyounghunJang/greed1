#각자리 숫자의 합 또는 곱으로 최대값 만들기 문제 


data=input()

result=int(data[0])

for i in range(1, len(data)):
    if(result<=1 or int(data[i])<=1):
        result+=int(data[i])
    else:
        result*=int(data[i])
print(result)
    
        
